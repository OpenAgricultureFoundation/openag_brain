API
===

The `openag_brain` project contains a REST API that allows for interacting with
ROS though HTTP. It is accessible at `<hostname>:5984/_openag/` when the
project is running.

Endpoints
---------

.. automodule:: openag_brain.api
    :members:
