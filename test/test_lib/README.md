Tests for `openag_lib`
======================

This python test code can be run like so:
`py.test ~/catkin_ws/src/openag_brain/test/`

This code was formerly in a separate repo called `openag_python`.
